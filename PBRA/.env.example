# Copy to .env and fill values. Do NOT commit .env with real secrets to source control.
DB_SERVER=db
DB_ROOT_PASSWORD=change_me_root_password
DB_USERNAME=regular_user
DB_PASSWORD=change_me_user_password
DB_DATABASE=pbradatabases

# SMTP for outgoing mail
SMTP_HOST=smtp.gmail.com
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD="your-smtp-password-or-app-password"
SMTP_PORT=587

# If you use Mailtrap (https://mailtrap.io/) for development/testing, you can set
# the MAILTRAP_* variables instead. These will be preferred by the app when present.
# Example (replace with values from your Mailtrap inbox credentials):
# MAILTRAP_HOST=smtp.mailtrap.io
# MAILTRAP_USERNAME=000000000000000
# MAILTRAP_PASSWORD="your-mailtrap-password"
# MAILTRAP_PORT=2525

# You can also send mail via an API instead of SMTP. Set MAILER_DRIVER to 'smtp' (default),
# 'sendgrid', or 'mailgun'. When using 'sendgrid', set SENDGRID_API_KEY. For 'mailgun', set
# MAILGUN_DOMAIN and MAILGUN_API_KEY.
# MAILER_DRIVER=smtp
# MAILER_DRIVER=sendgrid
# SENDGRID_API_KEY="your-sendgrid-api-key"
# MAILER_DRIVER=mailgun
# MAILGUN_DOMAIN=mg.example.com
# MAILGUN_API_KEY="your-mailgun-api-key"

# Mailtrap also exposes an HTTP send API. To use Mailtrap's API set MAILER_DRIVER=mailtrap
# and provide your MAILTRAP_API_TOKEN. Optionally override MAILTRAP_API_URL (default is
# Mailtrap send API endpoint). Example (use values from Mailtrap UI):
# MAILER_DRIVER=mailtrap
# MAILTRAP_API_TOKEN="your-mailtrap-api-token"
# MAILTRAP_API_URL="https://send.api.mailtrap.io/api/send"

# Optional: application timezone
APP_TZ=Asia/Singapore

# MySQL container initialization (the MySQL image expects these variables).
# Add these to ensure the DB initializes correctly when the container starts.
# Note: keep .env out of source control (add to .gitignore).
MYSQL_ROOT_PASSWORD=change_me_root_password
MYSQL_DATABASE=pbradatabases
MYSQL_USER=regular_user
MYSQL_PASSWORD=change_me_user_password

# For application compatibility, you can keep both DB_* and MYSQL_* keys
# Example app DB keys (some app files read DB_*):
DB_SERVER=db
DB_USERNAME=regular_user
DB_PASSWORD=change_me_user_password
DB_DATABASE=pbradatabases


# -------------------------
# SMTP2GO (example)
# -------------------------
# Sign up at https://www.smtp2go.com and use the SMTP username/password they provide.
# Recommended ports: 2525 or 587 (submission). Use 465 for implicit SSL if needed.
MAILER_DRIVER=smtp

# SMTP2GO settings
SMTP_HOST=smtp.smtp2go.com
SMTP_USERNAME=your_smtp2go_username
SMTP_PASSWORD=your_smtp2go_password
SMTP_PORT=2525
# Set to 'tls' for STARTTLS on 2525/587, 'ssl' for implicit TLS on 465, or 'none' for plain.
SMTP_ENCRYPTION=tls

# Default From address used by the test script and mailer
MAIL_FROM=noreply@your-domain.com
MAIL_FROM_NAME="Your App Name"

# Enable verbose mail logging for debugging (optional)
MAIL_DEBUG=true